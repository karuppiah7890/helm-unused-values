package pkg

// FindUnusedValues finds unused values
func FindUnusedValues(templatesPath string, values map[string]interface{}) {
	// walk the parsed template and look for chain nodes which start with
	// .Values
}
